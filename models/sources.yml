version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment


  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: orders   
        freshness: 
          error_after:
            count: 60
            period: day
          warn_after: 
            count: 30
            period: day
        loaded_at_field: _etl_loaded_at
        
      - name: customers